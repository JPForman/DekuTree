<!DOCTYPE html>
<html>
<head>
  <title>LuigisGrubReviews</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body>
  <div class="nav-bar table-responsive" id="navBar">
    <table class="table">
      <th><%= link_to "PRODUCTS", products_path%></th>
      <% if user_signed_in? %>
      <th class="authenticate"><%= current_user.email %>
      <th class="authenticate">
        <% if current_user.admin == true %>
        <%= "Admin"  %>
        <% elsif current_user.admin == false %>
        <%= "User"  %>
        <% end %>
      <th class="authenticate"><%= link_to "SIGN-OUT", destroy_user_session_path, :method => :delete%></th>
        <% else %>
      <th class="authenticate"><%= link_to "Sign-In/Sign-Up", new_user_session_path%></th>
        <% end %>
    </table>
  </div>
  <div class="container" id='title_container'>
    <h1 id="mainTitle">Welcome to </h1>
    <h1 id="title">The Deku Tree!</h1>
  </div>

      <%= flash[:alert] %>
      <%= flash[:notice] %>

      <div class="container" id="pallete">

        <h2 id="store_info">We have all the best products from around the world! <br>Leave a review about an adventure item you have used, or check out what others have to say!</h2>

        <div class="row container" id="contentRows">

          <div class="col-8 products">
            <div class="subTitle">
            <%= yield %>
          </div>
            </div>

          </div>
        </div>


</body>
</html>
